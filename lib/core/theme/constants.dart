import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF904C6E);
  static const Color accent = Color(0xFF262951);
  static const Color fieldFill = Colors.white;
  static const Color success = Colors.green;
  static const Color error = Colors.red;
}

class AppDimensions {
  static const double borderRadius = 12.0;
  static const double fieldBorderWidth = 1.5;
  static const double iconSpinnerSize = 16.0;
}
